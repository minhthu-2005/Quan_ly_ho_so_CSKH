<?xml version="1.0" encoding="utf-8"?>

<odoo>

    <record id="crm_lead_form_inherit"
            model="ir.ui.view">

        <field name="name">
            crm.lead.form.inherit
        </field>

        <field name="model">
            crm.lead
        </field>

        <field name="inherit_id"
               ref="crm.crm_lead_view_form"/>

        <field name="arch" type="xml">

            <xpath expr="//notebook"
                   position="inside">

                <page string="Lịch sử tương tác">

                    <field name="interaction_history_ids">

                        <tree editable="bottom">

                            <field name="interaction_type"/>
                            <field name="interaction_date"/>
                            <field name="content"/>
                            <field name="next_action"/>
                            <field name="followup_deadline"/>

                        </tree>

                    </field>

                </page>

            </xpath>

        </field>

    </record>

</odoo>